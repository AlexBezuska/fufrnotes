# Local/dev example
# For server deploy, create a .env in your app's server directory (see .deploy.env + scripts/servertron/*)

# Cookie security:
# - default: auto-detect via req.secure / X-Forwarded-Proto (recommended)
# - set COOKIE_SECURE=0 for plain-http local dev
# - set COOKIE_SECURE=1 to force Secure cookies
# COOKIE_SECURE=

# Postgres
FUFNOTES_DB_PASSWORD=CHANGE_ME

# Passhroom (server-side)
PASSHROOM_BASE_URL=
PASSHROOM_CLIENT_ID=
# PASSHROOM_CLIENT_SECRET=... (keep server-side only)
PASSHROOM_CALLBACK_URL=

# Local dev only: bypass external auth by forcing a user id
# DEV_USER_ID=dev-user
